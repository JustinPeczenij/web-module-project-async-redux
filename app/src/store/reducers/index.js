

const initialState = {
    isLoading: false,
    Quote: '',
    error: ''
} 

export const reducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}